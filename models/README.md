# Trained Model Artifacts

This directory contains the serialized artifacts used by the machine learning alert-prioritization pipeline.

The artifacts include the trained Random Forest classifier, the ordered feature schema used during inference, and the selected classification threshold.

These files allow the live SOC integration to process incoming security alerts consistently with the model training pipeline.
